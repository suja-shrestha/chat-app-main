// App.jsx is the ROOT component — every other component lives inside it
function App() {
  return (
    <div>
      <h1>Chat App is working! 🚀</h1>
    </div>
  );
}

// We export it so main.jsx can use it
export default App;